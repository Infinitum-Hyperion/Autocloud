part of autocloud.ui;

class AutocloudHomeView extends StatelessWidget {
  const AutocloudHomeView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const ACPGlobalScaffold(
      child: SizedBox(),
    );
  }
}
